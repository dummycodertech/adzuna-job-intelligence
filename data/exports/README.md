# This directory holds flat CSV exports for Power BI consumption.
# Files are generated by scripts/export_csv.py after each ingestion run.
# They are committed to the repo so Power BI can connect without an ODBC driver.
